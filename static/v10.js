(()=>{const root=document.documentElement;document.querySelectorAll('.v10-card,.kpi,.map-node').forEach((el,i)=>{el.classList.add('v10-reveal');el.style.setProperty('--delay',`${Math.min(i,10)*28}ms`)});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.05});document.querySelectorAll('.v10-reveal').forEach(e=>io.observe(e));document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();const links=[...document.querySelectorAll('.sidebar-nav a')];const names=links.map(a=>a.textContent.trim()).join('\n');alert('Quick navigation\n\n'+names+'\n\nCommand search UI is reserved for a future indexed search. No fake search results are shown.')}})})();
